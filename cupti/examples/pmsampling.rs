use anyhow::Context;
use cupti::pmsampling::*;
use cupti::profiler;
use cupti::profiler::HostProfiler;

// #[derive(clap::Params)]
// pub struct Args {
//     #[]
// }

fn main() -> anyhow::Result<()> {
    // let mut image = Sampler::get_counter_availability(0)
    //     .context("failed to get the counter availability image")?;
    // let mut chip = cupti::Device

    // let mut builder = Sampler::builder(chip_name, counter_availability_image)

    Ok(())
}
